// An instance of the browser window before node is taken away

// link electron to react (does this subvert nodeIntegration: false?)
// window.electron = require('electron')
window.require = require
